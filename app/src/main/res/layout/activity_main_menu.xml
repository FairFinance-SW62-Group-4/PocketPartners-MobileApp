<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="    #FFFFFF"
    tools:context=".MainMenu">

    <!-- Logo de la aplicación -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="500dp"
        android:background="@drawable/main_bakcground"
        android:orientation="vertical">

    <ImageView
        android:id="@+id/logoImageView"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_gravity="center"
        android:layout_marginTop="40dp"
        android:src="@mipmap/logo"
        android:contentDescription="Logo de la aplicación" />

    <!-- Título del menú -->
    <TextView
        android:id="@+id/titleTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginTop="16dp"
        android:text="Bienvenido a Pocket Partners!"
        android:textSize="20sp"
        android:textColor="#ffffffff"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/subtitleTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginTop="8dp"
        android:text="Gestiona tus finanzas de manera fácil y transparente"
        android:textSize="16sp"
        android:textColor="#ffffff" />

        <ImageView
            android:layout_width="match_parent"
            android:layout_height="250dp"
            android:layout_marginTop="18dp"
            android:contentDescription="Personas"
            android:scaleType="centerCrop"
            android:src="@mipmap/personas_foreground" />

    </LinearLayout>

    <!-- Botones de navegación o acción -->
    <Button
        android:id="@+id/login"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="50dp"
        android:background="@drawable/btn_naranja"
        android:text="Iniciar Sesión"
        android:textColor="#FFFFFF"
        android:layout_marginLeft="70dp"
        android:layout_marginRight="70dp"
        android:textSize="18sp" />

    <Button
        android:id="@+id/register"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:background="@drawable/btn_redondeado"
        android:text="Registrarse"
        android:textColor="#FFFFFF"
        android:layout_marginLeft="70dp"
        android:layout_marginRight="70dp"
        android:textSize="18sp" />

</LinearLayout>